//Return the Sum of Two Numbers
function addition(a, b) {
  return a + b;
}

// Function Factory
function makePlusFunction(baseNum) {
  return function (num) {
    return baseNum + num;
  };
}
Return - the - Sum - of - Two - Numbers;
function addition(a, b) {
  return a + b;
}
